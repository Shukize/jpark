# OS / editor cruft
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
*.swp
.vs/
node_modules/

# Environment secrets — ignore any .env file anywhere in the repo, but keep
# the committed .env.example templates.
.env
*.env
!*.env.example

# Python
__pycache__/
*.pyc

# Local-only photo backups (kept on disk, never used on the site)
*(Backup)/
my camera images/

# Local scratch / contact-sheet workspace
_work/
_orig_lobby/
